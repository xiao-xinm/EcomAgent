# frontend

前端工程集合目录，与 `backend/` 分离管理。

当前包含：

- `client-h5/`：用户端 H5 客服入口
- `app-h5/`：APP 内嵌 H5 客服入口
- `workstation/`：坐席工作台前端

当前阶段只保留工程配置和目录骨架，不写页面和业务交互代码。

## 常用命令

```bash
npm install
npm run dev:client-h5
npm run dev:workstation
```

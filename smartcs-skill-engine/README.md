# smartcs-skill-engine

技能注册与声明式编排模块，负责把 Agent 决策映射到可执行技能。

当前只保留工程骨架，后续可放置：

- 技能定义、技能注册表读取与缓存
- 意图到技能的路由关系
- API 调用链的参数映射、前置校验、回滚声明
- 技能执行状态事件

边界：

- 这里沉淀技能编排框架，不写具体电商业务逻辑
- 具体业务动作通过已有电商系统 API 或适配层提供
- 敏感技能只生成审核工单，不直接执行

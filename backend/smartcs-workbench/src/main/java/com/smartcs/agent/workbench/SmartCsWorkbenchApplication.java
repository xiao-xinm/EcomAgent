package com.smartcs.agent.workbench;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * SmartCS human workbench service entry point.
 */
@SpringBootApplication
public class SmartCsWorkbenchApplication {

    public static void main(String[] args) {
        SpringApplication.run(SmartCsWorkbenchApplication.class, args);
    }
}

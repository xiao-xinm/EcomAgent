# smartcs-knowledge

知识库与 RAG 检索增强模块，负责 FAQ、政策、流程说明等非实时知识。

当前只保留工程骨架，后续可放置：

- 知识文档管理
- 文档切片、向量化、索引构建
- 向量检索与关键词检索融合
- RAG 召回结果治理

边界：

- 订单状态、退款状态、物流轨迹等实时数据不走 RAG
- 实时数据必须通过业务 API 查询
- RAG 结果只作为回答依据，不直接触发业务动作

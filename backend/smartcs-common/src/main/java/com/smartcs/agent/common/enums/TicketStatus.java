package com.smartcs.agent.common.enums;

/**
 * Human work order lifecycle status.
 */
public enum TicketStatus {
    PENDING,
    ASSIGNED,
    PROCESSING,
    APPROVED,
    REJECTED,
    RESOLVED,
    CLOSED,
    ESCALATED
}
